<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Set the background color for the entire app -->
        <item name="android:windowBackground">@color/gray</item>
    </style>

    <style name="AppThemeSplash" parent="Theme.AppCompat.NoActionBar">
        <!-- Set the background color for the entire app -->
        <item name="android:windowBackground">@drawable/default_bg</item>
    </style>


    <style name="Base.Theme.Expence." parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Expence" parent="Base.Theme.Expence" />
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.Expence" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base.Theme.Expence" parent="Base.Base.Theme.Expence" />

    <style name="SheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <!-- <item name="android:windowCloseOnTouchOutside">true</item>-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@color/dim_foreground_disabled_material_dark</item>
       <!-- <item name="android:colorBackground">@android:color/transparent</item>-->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:windowFrame">@null</item>
        <!--<item name="android:windowAnimationStyle">@null</item>-->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
        <item name="android:windowSoftInputMode">adjustPan</item>
    </style>
</resources>